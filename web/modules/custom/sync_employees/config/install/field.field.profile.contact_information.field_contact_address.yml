langcode: ru
# Язык конфигурации.
status: true
# Активность конфигурации (true/false).
dependencies:
  # Конфиги и модули, от которых зависит инстанс поля.
  config:
    - field.storage.profile.field_contact_address
    - profile.type.contact_information
  module:
    - profile
    - sync_employees
id: profile.contact_information.field_contact_address
# Уникальный ID инстанса: entity.bundle.field_name.
field_name: field_contact_address
# Машинное имя поля.
entity_type: profile
# Тип сущности, к которой привязывается поле.
bundle: contact_information
# Бандл (тип профиля), на котором размещается поле.
label: 'Адрес'
# Заголовок поля в UI.
description: 'Адрес'
# Описание поля (подсказка).
required: false
# Обязательность заполнения.
translatable: false
# Переводимость значения поля.
default_value: { }
# Значение по умолчанию.
default_value_callback: ''
# Коллбек для значения по умолчанию (если используется).
settings: { }
# Настройки конкретного инстанса поля (обычно пусто для string).
field_type: string
# Тип поля должен соответствовать storage-конфигу.