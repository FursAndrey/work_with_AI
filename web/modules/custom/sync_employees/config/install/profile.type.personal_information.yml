langcode: ru
  # Язык конфигурации.
status: true
  # Конфигурация активна (true/false).
dependencies:
  # Зависимости по модулям, чтобы конфиг удалился вместе с модулем и имел корректный порядок установки.
  module:
    - profile
    - sync_employees
id: personal_information
  # Машинное имя типа профиля (bundle id).
label: 'Личная информация'
  # Человекочитаемое имя типа профиля.
registration: false
  # Показывать блок заполнения при регистрации пользователя.
multiple: false
  # Разрешено ли несколько профилей этого типа на одного пользователя.
weight: 0
  # Вес в списках (сортировка).
targetEntityType: user
  # Сущность-владелец, к которой привязан профиль (пользователь).
